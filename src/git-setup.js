//! git setup

// after installing git

// setup git envinorment which is git config
        // config variables can be stored in three different locations

        // system level ---> [path]/etc/gitconfig
        // user level ---> ~/.gitconfig
        // dir level  ----> .git/config

        // git config user.name yourname
        // git config user.email youremail

        // options :
            // --local
            // --global
            // --system

        // git config --list --show-origin ---> to know all env varibles for git

        //env vars
        // init.defaultBranch ---> default branch name

        // git config [key] ---> to Read values

//! ways to create a git repo

// 1.git init -----> initialize an git repo using
// 2.git clone [ssh/https url]-----> clone from the remote server




